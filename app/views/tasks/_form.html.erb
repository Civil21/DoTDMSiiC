<%= form_with model: [@project, task], id: "#{dom_id(task)}_form" do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :description %>
  <%= f.hidden_field :topic_id%>
  <%= f.button :submit, class: "btn btn-primary"%>
<% end %>
